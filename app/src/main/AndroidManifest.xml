<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studyforever">

    <!--demo里面需要上传文件,所以需要网络权限-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Retrofit.RetrofitActivity" />
        <activity android:name=".anim.AnimationDemo" />
        <activity android:name=".ui.SmileViewActivity" />
        <activity android:name=".ui.MRichTextActivity" />
        <activity android:name=".ui.TestSurfaceView" />
        <activity android:name=".ui.DeviceInfoActivity" />
        <activity android:name=".ui.ListViewActivity" />
        <activity android:name=".ui.PopupActivity" />
        <activity android:name=".ui.VolleyStudy" />
        <activity android:name=".network.okhttp.OkhttpActivity"/>
        <activity android:name=".network.retrofit.RetrofitActivity"/>
        <activity android:name=".dagger.DaggerActivity"/>
    </application>

</manifest>